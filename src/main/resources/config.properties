elasticsearch.host=linux-os
elasticsearch.port=9200
elasticsearch.scheme=https
elasticsearch.username=elastic
elasticsearch.password=1vuGTIHJDCovMHcsXvn+

# Producer configuration
elastic.producer.index=audit-logs
elastic.producer.batch-size=100
elastic.producer.flush-interval=5000
elastic.producer.records-per-day=10000

# Consumer configuration  
elastic.consumer.source-index=audit-logs
elastic.consumer.offset-index=consumer-offsets
elastic.consumer.consumer-id=consumer-1
elastic.consumer.batch-size=100
elastic.consumer.poll-interval=5000
elastic.consumer.scroll-timeout=1m

# Health and metrics
health.port=8080
metrics.enabled=true
